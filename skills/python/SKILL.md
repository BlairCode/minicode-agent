# Python

- Follow the repository's style; otherwise use PEP 8 and explicit type hints at public boundaries.
- Prefer the standard library and pytest for tests.
- Keep imports and exceptions precise. Avoid broad refactors unrelated to the task.

