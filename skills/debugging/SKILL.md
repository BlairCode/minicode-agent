# Debugging

- Form a small evidence-based hypothesis from the current error.
- Inspect the nearest relevant code and change one cause at a time.
- Rerun the failing command after each fix and stop repeating an unchanged action.

