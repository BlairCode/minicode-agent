# C++

- Preserve the configured language standard and existing style.
- Prefer RAII and standard containers; avoid non-portable extensions unless the project already uses them.
- Compile with warnings enabled when practical and test boundary cases.

